{
  "name": "memomuhub-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MEMOMUhub blockchain gaming platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:monad": "hardhat run scripts/deploy.js --network monad",
    "deploy:treasury:monad": "hardhat run scripts/deploy-treasury.js --network monad",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:treasury:local": "hardhat run scripts/deploy-treasury.js --network localhost",
    "node": "hardhat node"
  },
  "keywords": [
    "blockchain",
    "gaming",
    "monad",
    "smart-contracts",
    "web3"
  ],
  "author": "Nhom1984",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  }
}